启动流程说明 先跑bootloader（bootasm.S和bootmain.c），把内核从硬盘读到内存，然后跳转到内核入口entry.S，最后执行main()函数。 BIOS → Boot Sector → bootasm.S → bootmain.c → entry.S → main()

运行结果 [BOOT] enter bootmain [BOOT] elf header loaded [BOOT] kernel loaded
[KERNEL] main() started

个人总结 通过实证方法验证了操作系统启动过程的多阶段特性，证实了启动过程对CPU工作模式、内存管理单元等硬件状态的严格依赖关系，证明了ELF作为标准可执行格式在系统启动中的关键作用。 为操作系统内核开发提供了可靠的启动框架参考，建立的调试方法可用于后续内核模块的故障诊断，启动时间优化方案可为嵌入式系统开发提供借鉴。


# xv6 内核实践实验报告

## 一、实验内容说明

**完成层级：**
- 第一层（必做）：机制观察，全部完成  
- 第二层（选做）：系统调用行为修改（write 增加进程 PID 前缀）  

**具体任务：**
1. 系统调用路径跟踪（`trace_test` → `syscall.c` → `sysfile.c`）  
2. 调度与内存分配观察（`proc.c` 加 pid 日志，`kalloc.c` 加页地址日志）  
3. 修改 `sys_write`，所有输出自动带 `[PID=xx]`  

---

## 二、实现过程与截图（第二层任务）

### 修改目标
在 `write` 系统调用输出任何数据前，先打印当前进程的 PID。

### 修改方式
仅修改 `sysfile.c` 中的 `sys_write()`，调用 `filewrite` 之前加一行：
```c
cprintf("[PID=%d] ", proc->pid);


## 三、问题与解决
Windows 下无法 make → 用虚拟机 Ubuntu 编译

## 四、实践心得

通过本次实验，我完整跟踪了从 printf 出发的用户态代码，到触发系统调用进入内核的过程。在 syscall.c 中看到统一分发，再到 sys_write 完成实际写操作，理解了操作系统为安全隔离而设计的“用户态-内核态”切换路径。调度日志让我直观感受到进程切换频繁发生，即使执行简单命令，也会出现多个进程交替运行，同一个进程可能连续获得 CPU 的轮转现象。内存分配跟踪则揭示了物理页面的复用机制，新分配地址常是刚释放的旧页。修改系统调用行为只加了一行代码却影响了整个系统的输出，这让我体会到内核的高度可定制性。整个过程从视频学习到动手修改再到观察结果，将抽象概念转化为可运行、可验证的实例，加深了我对操作系统运行机制的理解，也为以后学习更复杂的同步、驱动等知识积累了信心。